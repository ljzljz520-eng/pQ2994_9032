{
  "name": "scanvault-console",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true}); require('fs').writeFileSync('dist/index.html','<!doctype html><title>ScanVault</title><main>ScanVault console</main>')\""
  }
}
